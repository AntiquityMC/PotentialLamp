# PotentialLamp's intermediary mappings

These intermediary mappings are for usage with Loom.
They differ from the `master` branch's mappings in that
the obfuscated namespace is `client` instead of `official`.
